# Copy to your .env file (PUT YOUR .env ON YOUR .gitignore!!!)

# === Development Database ===
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=conf

# === Test Database ===
# Used when running tests with NODE_ENV=test
# Uses same PostgreSQL instance, different database name
DB_TEST_HOST=localhost
DB_TEST_PORT=5432
DB_TEST_USER=postgres
DB_TEST_PASSWORD=postgres
DB_TEST_NAME=conf_test

# === Application ===
BACKEND_PORT=3000
NODE_ENV=development

FRONTEND_PORT=5173

VITE_API_URL=http://localhost:3000

# The script setup.sh generate a unic value for you
# JWT_SECRET=auto_generate

# === Test Environment (optional - override for tests) ===
# When running tests, these values will be used:
# NODE_ENV=test
# DB_HOST=localhost
# DB_PORT=5433
# DB_NAME=conf_test
# BACKEND_PORT=3001